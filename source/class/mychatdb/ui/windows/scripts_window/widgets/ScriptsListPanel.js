qx.Class.define("mychatdb.ui.windows.scripts_window.widgets.ScriptsListPanel", {
    extend: mychatdb.ui.widgets.ListPanel,

    properties: {
        // server: {init: null}
    },

    members: {

        // initialize: function (parentWindow) {
        //     this.base(arguments, parentWindow);
        //     this.setServer(new mychatdb.data.server.Server());
        // },

        // clear: function () {
        //     this.setValues([]);
        // },

        // refresh: function () {
        //     this.base(arguments);
        //     const fn = (data) => {
        //         this.setValues(data);
        //     };
        //     this.getServer().getScripts(fn);
        // },

        // setValues: function (rawData) {
        //     const model = qx.data.marshal.Json.createModel(rawData);
        //     this.setModel(model);
        // }

    }
});
